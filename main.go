package main

import (
	"fmt"
	"os"
)

func main() {
	// get current dir

	// look through it for dirs matching 'gs-'
	// find one with max number
	// create new dir 'gen-<num + 1>'
	fmt.Println(os.Args[0])
}
